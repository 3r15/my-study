# 3번 영역 설계 — 오답 분석 · 취약점 기록 · 문제 생성

요청하신 요구사항은 셋으로 나뉩니다.

1. **각 문제별로 "내가 어떤 선택지를 골랐는지"에 따라 "무엇을 모르는지" 판단해서 저장**
2. 그 취약점 + 내가 직접 적은 "모르는 내용"을 **기록**
3. 그 기록을 **써서(또는 안 쓰고 랜덤으로) 문제를 더 생성**

아래는 지금 구현해 둔 방식과, 남은 선택지들의 트레이드오프입니다.
**현재 v1(선택지 자동 태깅)은 이미 동작합니다.** 2·3단계는 원하시면 추가하면 됩니다.

---

## 핵심 질문: "선택지 → 오개념"을 누가 붙이는가

이게 이 영역 전체의 설계를 가릅니다. 세 가지 방법이 있습니다.

| | 방법 | 정확도 | 비용 | 확장성 |
|---|---|---|---|---|
| **v1** | 선택지 텍스트 ↔ 요약집 항목 **자동 매칭** | 중~상 | 0 (빌드 때 끝) | 720문항 즉시 |
| **v2** | 선택지별 오개념을 **LLM이 한 번 생성**해 JSON에 굽기 | 상 | 1회 배치 (2,880개) | 파일만 커짐 |
| **v3** | 틀릴 때마다 **내가 직접 태깅** | 최상 | 내 시간 | 느리지만 정확 |

현재는 **v1을 구현**하고, **v3을 메모 형태로 병행**할 수 있게 해뒀습니다.

---

## v1 (구현 완료) — 선택지 자동 개념 태깅

### 왜 이게 되는가

정보처리산업기사 4지선다는 대부분 **"같은 계열의 다른 개념"을 오답으로 씁니다.**

> **8.** 디자인 패턴 중 Singleton에 대한 설명으로 옳은 것은?
> ① 하나의 객체를 생성하면 어디서든 참조할 수 있지만… ← **싱글톤**
> ② 원본 객체를 복제하는 방법으로 객체를 생성하는… ← **프로토타입**
> ③ 여러 객체를 가진 복합 객체와 단일 객체를 구분 없이… ← **컴포지트**
> ④ 수많은 객체들 간의 복잡한 상호작용을 캡슐화하여… ← **중재자/빌더**

②를 골랐다면 "싱글톤을 모른다"가 아니라 **"프로토타입을 싱글톤이라고 알고 있다"**가 정확한 진단입니다.
그리고 각 선택지가 어떤 개념인지는 **선택지 텍스트만 봐도 알 수 있습니다** — 요약집 193항목 중 어디에 나오는 문장인지 찾으면 되니까요.

### 구현

빌드(`build_data.py`)에서 TF-IDF 유사도로 매칭합니다.

```
요약집 193항목 각각에 대해 키워드 벡터 생성
  = 제목 토큰(가중치 4) + 본문 토큰(가중치 1)
  토큰 = 영단어 + 한글 2글자 이상 어절 + 4글자 이상 어절의 바이그램
  (불용어 제거: "것은/설명/다음/옳지 않은" 등 문제 상투어)

각 선택지 텍스트에 대해
  score(항목) = Σ min(선택지_토큰빈도, 2) × 항목_가중치 × idf(토큰)
  같은 과목이면 × 1.6  ← 과목이 달라도 매칭은 되게(E-R 기호 문제가 1과목에 나오는 경우 등)
  상위 2개를 options[].refs 로 저장
```

문항 전체(`refs`)도 같은 방식으로 상위 3개를 저장하고, 그중 1위 항목이 속한 구간으로 **단원(unit)**을 정합니다.

### 점수 모델

```js
function grade(q, sel) {
  const ok = (sel === q.answer);
  if (!ok) {
    q.options[sel-1].refs.forEach(n => weak[n] += 3);  // 이걸 옳다고 착각
    q.options[q.answer-1].refs.forEach(n => weak[n] += 2);  // 이걸 몰랐다
    weak[q.refs[0]] += 2;                              // 주제 자체가 약함
  } else {
    q.refs.forEach(n => weak[n] -= 1);                 // 회복 (0 미만 안 내려감)
  }
}
```

**왜 이 가중치인가**
- 오답 선택지(+3) > 정답 선택지(+2) — 잘못 알고 있는 것이 모르는 것보다 위험합니다. 시험장에서 자신 있게 틀립니다.
- 회복이 −1로 느린 것은 의도적입니다. 한 번 맞혔다고 안다고 볼 수 없어서, 3~4번 연속 맞혀야 취약 목록에서 빠집니다.
- 점수를 0 밑으로 안 내리는 이유: 잘하는 개념이 음수로 깊어지면 나중에 한 번 틀렸을 때 반응하지 않습니다.

### 결과 (200문항 시뮬레이션, 오답률 40%)

취약 개념 100개가 잡히고, 상위권이 실제로 특정 요약 항목(사용자 정의 함수, 효과적인 모듈 설계 방안, DCL 등)으로 수렴합니다. 오답분석 화면의 「선택지로 본 오개념」이 이걸 개념별로 묶어 보여줍니다.

```
[3회] 생성 패턴 - 프로토타입(Prototype)          [요약 보기]
  · 2024-1-08 — 나는 ② 원본 객체를 복제하는 방법으로… 선택 (정답 ①)
  · 2023-2-11 — 나는 ② 프로토타입 모형은… 선택 (정답 ④)
  [이 개념 문제 풀기]
```

### 한계 (솔직하게)

- **계산 문제**에는 안 통합니다. "평균 반환 시간은?" ① 9.25 ② 8.25 — 선택지가 숫자라 개념이 안 나옵니다. 이런 문항은 4개 선택지가 모두 같은 refs를 갖고, 사실상 "문항 주제 +2"만 동작합니다. (표·코드가 있는 문항 83개가 대체로 여기 해당)
- ~~**부정형 문항**에서는 의미가 뒤집힙니다~~ → **해결.** 지문에 `아닌|않는|틀린|잘못|거리가 먼`이 있으면 정답 선택지 가중치(+2)를 적용하지 않습니다.
- 매칭 실패 5문항 (요약집에 대응 항목이 없는 신규 출제).

---

## v2 (권장 다음 단계) — LLM으로 오개념을 한 번 굽기

v1의 한계는 "선택지 텍스트만 본다"는 데서 옵니다. **왜 이 선택지가 오답인지**는 텍스트에 없습니다.

### 방법

720문항을 배치로 돌려 각 선택지에 사람이 읽을 수 있는 오개념 한 줄을 붙입니다.

```json
{
  "qid": "2024-1-08",
  "explain": "②번은 어댑터(Adapter) 패턴에 대한 설명입니다.",
  "misread": [
    null,
    "프로토타입 패턴(객체 복제)과 싱글톤(단일 인스턴스 공유)을 혼동",
    "컴포지트 패턴(복합/단일 객체 동일 취급)을 싱글톤으로 착각",
    "중재자 패턴(객체 간 상호작용 캡슐화)을 싱글톤으로 착각"
  ],
  "concepts": [54, 53, 55, 52]
}
```

- 필드만 추가하면 되므로 **기존 스키마·UI 그대로** 쓸 수 있습니다. `fbHtml()`에 두 줄 추가.
- **2024년 3개 회차 PDF에는 이미 해설이 들어 있습니다.** 다만 해설의 문항 번호가 이미지로 렌더링돼 있어 자동 매핑이 안 됩니다 — 순서대로 매칭 + OCR로 검증하면 180문항은 공짜로 얻을 수 있습니다.
- 비용: 720문항 × 약 300토큰 = 한 번에 끝나는 배치. 파일은 문항당 ~200바이트 늘어 총 +150KB 수준.

### 얻는 것
- 정확한 오개념 문구 (계산·부정형 문항 포함)
- 해설 표시 → 지금 없는 가장 큰 결핍
- v1 점수 모델을 그대로 쓰되 `concepts`를 refs 대신 사용 → 정확도 상승

---

## v3 — 내가 직접 기록 (지금도 부분 동작)

자동 태깅이 못 잡는 걸 메꾸는 안전장치입니다.

**지금 되는 것**
- 문항별 메모 (`notes[qid]`) — 채점 후 "✎ 모르는 내용 메모하기"
- 요약 항목별 메모 (`secNotes[secNo]`) — 개념 페이지 하단
- "모르겠음" 버튼 — 찍어서 맞히는 걸 방지하고 주제에 +2
- 오답분석 화면에서 메모 모아보기

- 채점 직후 **"왜 틀렸나요?"** 3버튼 — `개념을 몰랐다` / `헷갈렸다` / `단순 실수`
  - **단순 실수**를 누르면 방금 올라간 취약 점수를 그대로 되돌립니다 → 오탐 제거
  - **개념을 몰랐다**는 문항 주제에 +2를 더 얹습니다
  - 클릭 한 번이라 부담이 적고, 자동 태깅의 가장 큰 약점(계산 실수를 개념 부족으로 오인)을 메꿉니다

**추가하면 좋은 것 (미구현)**
- 메모에 `#키워드`를 쓰면 자동으로 태그가 되어 취약 개념과 별개의 축으로 집계
- `헷갈렸다`를 고른 문항끼리 묶어 "혼동 쌍"(싱글톤↔프로토타입 같은) 목록 만들기

---

## 문제 생성 — 3가지 경로

### A. 기출 재출제 (구현 완료)
취약 상위 12개 개념에 연결된 기출을 뽑아 **미풀이 → 틀림 → 맞힘** 순으로 정렬. 새 문제가 아니라 "안 본 문제 + 틀린 문제"를 개념 기준으로 재조합하는 것이라 가장 실전에 가깝습니다.

### B. 요약집 기반 자동 생성 (구현 완료, 오프라인)
요약집 불릿과 용어 정의를 재료로 씁니다.

| 유형 | 형태 | 재료 |
|---|---|---|
| `notin` | "다음 중 「XXX」에 해당하지 않는 것은?" | 해당 항목 불릿 3 + 타 항목 불릿 1 |
| `define` | 정의를 주고 용어 맞히기 | 용어사전 |
| `match` | 용어를 주고 정의 맞히기 | 용어사전 |

품질 장치:
- 오답 보기는 **같은 단원** 우선 (없으면 같은 과목) — 안 그러면 "UML 관계" 문제에 "주기억장치 분할" 보기가 섞여 너무 쉬워집니다
- 정답 보기와 **머리말(: 앞)이 겹치면 제외** — "전송 계층 : TCP, UDP" vs "전송 계층 : 종단 시스템 간…" 같은 복수정답 방지
- 예제 풀이 과정(`1회전`, `초기 상태`)·숫자 비율 30% 초과 불릿은 재료에서 제외
- 자동 검증 600문항 → 구조적 결함 0건

**한계**: 문장을 재배열할 뿐 새 개념을 만들지는 못합니다. 암기 확인용으로는 충분하지만, 계산·코드 문항은 만들 수 없습니다.

### C. AI 연동 (구현 완료 — 프롬프트/JSON 왕복)
정적 사이트라 API 키를 넣을 수 없으므로 **복사·붙여넣기 왕복**으로 처리합니다.
「프롬프트 복사」 → 취약 개념 + 메모 + 최근 오답 25개가 담긴 프롬프트가 클립보드로 →
Claude에 붙여넣기 → 받은 JSON을 「생성 JSON 불러오기」에 붙여넣으면 그 자리에서 풀립니다.

계산·코드 문항까지 만들 수 있는 유일한 경로이고, 지금 구조에서 서버 없이 가능한 최선입니다.

**서버를 쓸 수 있다면**: Cloudflare Workers 같은 데 키를 두고 프록시하면 왕복 없이 버튼 하나로 됩니다. 다만 학습 기록이 로컬에만 있는 지금 구조의 단순함은 잃습니다.

---

## 정리 — 무엇을 먼저 할지

| | 작업 | 효과 | 상태 |
|---|---|---|---|
| ✅ | 선택지 자동 개념 태깅 (v1) | 선택지별 오개념 기록 | 구현 완료 |
| ✅ | **"왜 틀렸나요?" 3버튼** | 실수/개념부족 분리 → 취약 점수 오탐 급감 | 구현 완료 |
| ✅ | **부정형 문항 가중치 보정** | `아닌/않는/틀린` 문항에서 정답 refs +2 제거 | 구현 완료 |
| ✅ | 요약집 기반 문제 생성 + AI 프롬프트 왕복 | 3번 영역 생성부 | 구현 완료 |
| 1 | **2024년 PDF 해설 추출** (3회차 180문항) | 해설 표시 — 현재 가장 큰 결핍 | 반나절 (번호 이미지 OCR 필요) |
| 2 | **v2 배치 오개념 생성** (720×4) | 오답 분석 정확도 최상 | 배치 1회 |
| 3 | 구 체계 기출(2005~2021) 추가 | 문항 수 3배 | 파서 대부분 재사용 |
| 4 | 혼동 쌍 목록 / 메모 해시태그 | 자기 기록 축 강화 | 반나절 |
